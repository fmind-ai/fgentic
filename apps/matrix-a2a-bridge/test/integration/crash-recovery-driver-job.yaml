apiVersion: batch/v1
kind: Job
metadata:
  name: crash-recovery-driver
  namespace: bridge-integration
spec:
  activeDeadlineSeconds: 600
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: crash-recovery-driver
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      securityContext:
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: driver
          image: matrix-a2a-bridge-integration:local
          imagePullPolicy: Never
          command: [/usr/local/bin/integration-driver]
          env:
            - name: DRIVER_SCENARIO
              value: crash-recovery
            - name: DRIVER_TIMEOUT
              value: 570s
            - name: MATRIX_URL
              value: http://synapse:8008
            - name: BRIDGE_URL
              value: http://bridge:29331
            - name: BRIDGE_METRICS_URL
              value: http://bridge:9090/metrics
            - name: BRIDGE_DATABASE_URL
              value: postgres://postgres@postgres:5432/bridge?sslmode=disable
            - name: A2A_STUB_URL
              value: http://plain-a2a-agent.plain-agent.svc.cluster.local:8080
            - name: FAULT_PROXY_URL
              value: http://fault-proxy:8081
            - name: MATRIX_SERVER_NAME
              value: integration.test
            - name: BRIDGE_HS_TOKEN
              value: integration-homeserver-token
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 250m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
