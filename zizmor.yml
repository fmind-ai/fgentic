rules:
  template-injection:
    ignore:
      # The offline smoke contract still matches these GitHub-controlled outcome markers. The
      # executable shell consumes the same fixed enum values through the step-level env boundary.
      - smoke.yml:195:41
      - smoke.yml:195:89
      - smoke.yml:215:60
