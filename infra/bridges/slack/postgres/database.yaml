apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: slackbridge
  namespace: postgres
spec:
  cluster:
    name: platform-pg
  name: slackbridge
  owner: slackbridge
  encoding: UTF8
  # Removing the optional component must not erase provider sessions/history implicitly.
  databaseReclaimPolicy: retain
