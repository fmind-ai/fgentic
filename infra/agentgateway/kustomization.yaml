# Offline composition of the stable agentgateway base and the separately ordered A2A admission
# boundary. Flux inventories target these subdirectories directly during the staged #339 handoff.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base
  - admission
