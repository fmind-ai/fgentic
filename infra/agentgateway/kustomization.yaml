# Offline composition of the common agentgateway base and A2A admission layers. Flux reconciles
# these directories separately so provider changes can sequence backend -> admission -> egress.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base
  - admission
