# The staged Azure OpenAI egress inventory intentionally renders no objects.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - placeholder.yaml
patches:
  - target:
      kind: ConfigMap
      name: agentgateway-azure-openai-egress-placeholder
    patch: |
      $patch: delete
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: agentgateway-azure-openai-egress-placeholder
