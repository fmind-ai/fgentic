# Kustomize rejects a resource-free file. This build-only placeholder is deleted by the profile's
# patch, leaving Flux an empty inventory that prunes any previously enabled admin resources.
apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-disabled-placeholder
