# The probe authenticates to Matrix with a mounted credential, never the Kubernetes API, so it
# gets no projected token.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: delegation-canary
  namespace: canary
automountServiceAccountToken: false
