# Homeserver C is an independent, non-partner Flux unit used to prove that the closed federation
# rejects servers outside the A/B allowlist.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: matrix-c
  namespace: flux-system
spec:
  interval: 30m
  retryInterval: 2m
  path: ./infra/federation/matrix-c
  prune: true
  wait: true
  timeout: 20m
  dependsOn:
    - name: postgres
    - name: gateway
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: platform-settings
      - kind: ConfigMap
        name: platform-settings-overrides
        optional: true
