# Observability layer (SPEC §9): metrics, OTLP collection, and in-cluster trace storage.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - tracing-helmreleases.yaml
  - tracing-networkpolicy.yaml
  - grafana-route.yaml
