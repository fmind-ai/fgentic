# Observability layer (SPEC §9): metrics stack + platform monitors + the LLM cost alert.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - grafana-route.yaml
