# The alert-delivery receiver exists only while the alert_delivery platform profile is enabled;
# disabling the profile lets Flux prune it completely (issue #456).
apiVersion: v1
kind: Namespace
metadata:
  name: alert-delivery
  labels:
    fgentic.dev/managed: "true"
    fgentic.dev/image-policy: enforce
    fgentic.dev/quota-profile: small
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
