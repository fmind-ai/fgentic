# The receiver authenticates to Matrix with a mounted access token, never the Kubernetes API.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: alert-receiver
  namespace: alert-delivery
automountServiceAccountToken: false
