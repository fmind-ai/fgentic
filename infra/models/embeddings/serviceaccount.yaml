# The embeddings/reranker serving engines and their one-shot model loaders need no Kubernetes API
# access. Shared by both HelmReleases and both loader Jobs; the token is never mounted.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: knowledge-embeddings
  namespace: models
automountServiceAccountToken: false
