# The embeddings/reranker data plane is closed by default. Only the gateway may submit tokenize/
# embed/score requests and only the labeled Prometheus workload may scrape; the serving Pods receive
# no egress at all.
# A distinct default-deny name lets this profile coexist with (or stand alone from) the vLLM chat
# profile's `models-default-deny` without a two-owner conflict on one object.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: models-embeddings-default-deny
  namespace: models
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: knowledge-embeddings-allow-platform-ingress
  namespace: models
spec:
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/instance
        operator: In
        values:
          - knowledge-embeddings
          - knowledge-reranker
    matchLabels:
      app.kubernetes.io/component: serving-engine
      app.kubernetes.io/part-of: vllm-stack
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: agentgateway-system
          podSelector:
            matchLabels:
              app.kubernetes.io/name: agentgateway-proxy
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
          podSelector:
            matchLabels:
              fgentic.dev/metrics-reader: prometheus
      ports:
        - protocol: TCP
          port: 8000
---
# Model bootstrap is the sole external exception. Standard NetworkPolicy cannot constrain HTTPS by
# FQDN, so these short-lived, prompt-free loader Jobs may reach public addresses but not private
# ranges. Scoped to the two embedding/reranker loaders by exact name.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: embeddings-model-loader-allow-download
  namespace: models
spec:
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: In
        values:
          - bge-m3
          - bge-reranker-v2-m3
    matchLabels:
      app.kubernetes.io/component: model-loader
      app.kubernetes.io/part-of: vllm-stack
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
            # Exclude non-global and special-use IPv4 space from the public download path.
            except:
              - 0.0.0.0/8
              - 10.0.0.0/8
              - 100.64.0.0/10
              - 127.0.0.0/8
              - 169.254.0.0/16
              - 172.16.0.0/12
              - 192.0.0.0/24
              - 192.0.2.0/24
              - 192.88.99.0/24
              - 192.168.0.0/16
              - 198.18.0.0/15
              - 198.51.100.0/24
              - 203.0.113.0/24
              - 224.0.0.0/3
      ports:
        - protocol: TCP
          port: 443
