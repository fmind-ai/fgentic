apiVersion: v1
kind: Namespace
metadata:
  name: kagent
  labels:
    # kagent's tool/agent pods need broader access than "restricted"; enforce baseline.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
