apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-overlay-validation
  namespace: flux-system
spec:
  interval: 1h
  path: ./
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    # Offline CI cannot resolve substituteFrom. These canonical sample values exercise the same
    # strict substitution surface while each overlay still carries its own platform-settings.
    substitute:
      server_name: ci.fgentic.example
      cluster_issuer: ci-issuer
      acme_email: ci@fgentic.example
      gcp_project: ci-fgentic
      pg_backups_bucket: ci-fgentic-pg-backups
      ingress_ip_name: ci-fgentic-ingress
      llm_provider: vertex
      llm_model: google/gemini-2.5-flash
      vertex_region: europe-west1
      openai_host: api.openai.com
      azure_openai_resource: not-configured
      llm_usage_budget_15m: "100000"
      quota_default_request_cpu: 25m
      quota_default_request_memory: 64Mi
      quota_default_limit_cpu: 500m
      quota_default_limit_memory: 512Mi
      quota_small_pods: "24"
      quota_small_requests_cpu: "4"
      quota_small_requests_memory: 8Gi
      quota_small_limits_cpu: "12"
      quota_small_limits_memory: 16Gi
      quota_core_pods: "32"
      quota_core_requests_cpu: "8"
      quota_core_requests_memory: 16Gi
      quota_core_limits_cpu: "24"
      quota_core_limits_memory: 24Gi
      quota_compute_pods: "16"
      quota_compute_requests_cpu: "8"
      quota_compute_requests_memory: 16Gi
      quota_compute_limits_cpu: "16"
      quota_compute_limits_memory: 24Gi
